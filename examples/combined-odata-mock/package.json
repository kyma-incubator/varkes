{
  "name": "@varkes/example-combined-odata-mock",
  "version": "0.6.2",
  "description": "combined example of odata and api-server",
  "main": "app.js",
  "private": true,
  "scripts": {
    "test": "mocha",
    "start:dev": "npm run clean && export DEBUG=true && node . 10000",
    "start": "node . 10000",
    "clean": "rm -rf logs || true && rm -rf data || true && rm -rf keys || true"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@varkes/api-server": "^0.6.2",
    "@varkes/cockpit": "^0.6.2",
    "@varkes/odata-mock": "^0.6.2",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^6.1.4",
    "supertest": "^4.0.2"
  },
  "repository": {
    "type": "ssh",
    "url": "git@github.com:kyma-incubator/varkes.git"
  }
}
