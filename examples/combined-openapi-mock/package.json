{
  "name": "@varkes/example-combined-openapi-mock",
  "version": "0.12.0",
  "description": "combined example of varkes openapi-mock and varkes api-server",
  "main": "app.js",
  "private": true,
  "scripts": {
    "test": "mocha",
    "start:dev": "npm run clean && export DEBUG=true && node . 10000",
    "start": "node . 10000",
    "clean": "rm -rf logs || true && rm -rf data || true && rm -rf keys || true && rm -rf generated || true"
  },
  "publishConfig": {
    "access": "public"
  },
  "author": "",
  "license": "Apache-2.0",
  "dependencies": {
    "@varkes/api-server": "^0.12.0",
    "@varkes/cockpit": "^0.12.0",
    "@varkes/configuration": "^0.12.0",
    "@varkes/openapi-mock": "^0.12.0",
    "express": "^4.17.1"
  },
  "devDependencies": {
    "mocha": "^8.2.1",
    "supertest": "^6.0.1"
  },
  "repository": {
    "type": "ssh",
    "url": "git@github.com:kyma-incubator/varkes.git"
  }
}
